# Assets

本网站先使用 CSS 生成的大量占位图片和视频视觉块。

后续替换真实素材时建议：

- 图片放入 `assets/images/`，可按 `placeholder-romance-01.jpg`、`placeholder-firework-01.jpg` 等名称替换。
- 视频放入 `assets/videos/`，可按 `placeholder-firework-01.mp4`、`placeholder-rain-01.mp4` 等名称管理。
- 主音乐当前直接引用根目录的 `心似烟火.mp3`。
