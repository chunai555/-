{
  "name": "houhuiwuci-youth-music-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/prepare-media.mjs && tsc && vite build && node scripts/build-standalone.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.5"
  }
}
